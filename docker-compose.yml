services:
  db:
    # 本机已有 mysql:8.0；与 MariaDB 协议兼容，避免额外拉 mariadb 镜像
    image: mysql:8.0
    environment:
      MYSQL_USER: mushroomshed
      MYSQL_PASSWORD: mushroomshed
      MYSQL_DATABASE: mushroomshed
      MYSQL_ROOT_PASSWORD: mushroomshed-root
    ports:
      - "3310:3306"
    volumes:
      - mariadata:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-pmushroomshed-root"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 40s

  backend:
    build: ./backend
    ports:
      - "8800:8800"
    environment:
      DATABASE_URL: mysql+pymysql://mushroomshed:mushroomshed@db:3306/mushroomshed
      JWT_SECRET: mushroom-shed-jwt-secret-change-me
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: "480"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    ports:
      - "3800:80"
    depends_on:
      - backend

volumes:
  mariadata:
